==========================================================================
string对象：
常用属性：length，例子："大家好".length

常用方法：
indexOf(subsString[,limit])
substr(start[,length])
substring(from[,to])
split(delimiter[,limit])